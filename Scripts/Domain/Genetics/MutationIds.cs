namespace Voidling.Domain.Genetics;

/// <summary>
/// Stable semantic IDs for mutation types that already have product behavior/presentation. These
/// identities may cross Application boundaries; display labels and visual treatment remain outside
/// Domain. Adding an ID here does not define inheritance or transformation rules by itself.
/// </summary>
public static class MutationIds
{
    public const string Angel = "Angel";
}
