@AGENTS.md

# Claude Code notes

- Use plan mode before broad cross-layer changes.
- `.claude/rules/` contains path-scoped constraints; keep those rules consistent with `ARCHITECTURE.md`.
- Do not copy durable architecture guidance into this file. Update the shared source and keep this import small.
